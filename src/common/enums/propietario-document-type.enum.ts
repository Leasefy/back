/**
 * Document types for property owners (propietarios).
 * Matches Prisma schema enum PropietarioDocumentType.
 */
export enum PropietarioDocumentType {
  CC = 'CC',
  CE = 'CE',
  NIT = 'NIT',
  PASSPORT = 'PASSPORT',
}
