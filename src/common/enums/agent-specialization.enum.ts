/**
 * Specialization areas for agents.
 * Matches Prisma schema enum AgentSpecialization.
 */
export enum AgentSpecialization {
  RESIDENTIAL = 'RESIDENTIAL',
  COMMERCIAL = 'COMMERCIAL',
  BOTH = 'BOTH',
}
