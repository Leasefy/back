---
phase: 05-scoring-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/common/enums/risk-level.enum.ts
  - src/common/enums/index.ts
  - src/config/env.validation.ts
  - src/scoring/scoring.module.ts
  - src/app.module.ts
autonomous: true
user_setup:
  - service: redis
    why: "BullMQ requires Redis for job queue persistence"
    env_vars:
      - name: REDIS_URL
        source: "Upstash Console -> Database -> REST API -> Connection String (redis://...)"

must_haves:
  truths:
    - "RiskScoreResult table exists in database"
    - "RiskLevel enum (A/B/C/D) available in TypeScript and Prisma"
    - "REDIS_URL environment variable validated on startup"
    - "ScoringModule registered in app"
    - "BullMQ queue 'scoring' configured"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "RiskScoreResult model and RiskLevel enum"
      contains: "model RiskScoreResult"
    - path: "src/common/enums/risk-level.enum.ts"
      provides: "RiskLevel TypeScript enum"
      exports: ["RiskLevel"]
    - path: "src/config/env.validation.ts"
      provides: "REDIS_URL validation"
      contains: "REDIS_URL"
    - path: "src/scoring/scoring.module.ts"
      provides: "ScoringModule with BullMQ registration"
      exports: ["ScoringModule"]
  key_links:
    - from: "src/scoring/scoring.module.ts"
      to: "BullMQ"
      via: "BullModule.registerQueue"
      pattern: "BullModule\\.registerQueue"
    - from: "src/app.module.ts"
      to: "src/scoring/scoring.module.ts"
      via: "imports array"
      pattern: "ScoringModule"
---

<objective>
Set up the foundation for the scoring engine: database model for results, Redis configuration for BullMQ, and the ScoringModule shell.

Purpose: Provide the infrastructure (persistence + queue) that all scoring components will use.
Output: RiskScoreResult model in database, RiskLevel enum, configured BullMQ queue.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scoring-engine/05-RESEARCH.md
@prisma/schema.prisma
@src/config/env.validation.ts
@src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RiskLevel enum and RiskScoreResult model to Prisma</name>
  <files>
    prisma/schema.prisma
    src/common/enums/risk-level.enum.ts
    src/common/enums/index.ts
  </files>
  <action>
1. Create `src/common/enums/risk-level.enum.ts`:
   - Export `RiskLevel` enum with values: A, B, C, D
   - Add JSDoc comments explaining each level:
     - A (80-100): Excellent - approve immediately
     - B (65-79): Good - approve with standard terms
     - C (50-64): Fair - approve with conditions
     - D (0-49): Poor - decline or require cosigner

2. Update `src/common/enums/index.ts`:
   - Add export for risk-level.enum.ts

3. Update `prisma/schema.prisma`:
   - Add `RiskLevel` enum with values A, B, C, D (after other enums)
   - Add `RiskScoreResult` model with fields:
     - id: String @id @default(uuid()) @db.Uuid
     - applicationId: String @unique @map("application_id") @db.Uuid
     - totalScore: Int @map("total_score")
     - level: RiskLevel
     - financialScore: Int @map("financial_score")
     - stabilityScore: Int @map("stability_score")
     - historyScore: Int @map("history_score")
     - integrityScore: Int @map("integrity_score")
     - signals: Json @default("[]")
     - drivers: Json @default("[]")
     - flags: Json @default("[]")
     - conditions: Json @default("[]")
     - explanation: String? @db.Text (for Phase 7 AI)
     - algorithmVersion: String @default("1.0") @map("algorithm_version")
     - createdAt: DateTime @default(now()) @map("created_at")
   - Add relation: application Application @relation(fields: [applicationId], references: [id], onDelete: Cascade)
   - Add indexes: @@index([applicationId]), @@index([level])
   - Map to table: @@map("risk_score_results")
   - Add relation back-reference in Application model: riskScore RiskScoreResult?
  </action>
  <verify>
    - Run `npx prisma validate` - should pass
    - Run `npx prisma format` - schema formatted
    - TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
    RiskLevel enum exists in both TypeScript (src/common/enums/risk-level.enum.ts) and Prisma.
    RiskScoreResult model defined with all score fields and JSON columns.
    Application model has optional riskScore relation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Redis configuration and BullMQ module setup</name>
  <files>
    src/config/env.validation.ts
    src/scoring/scoring.module.ts
    src/app.module.ts
  </files>
  <action>
1. Update `src/config/env.validation.ts`:
   - Add REDIS_URL validation: @IsString() REDIS_URL!: string;
   - Add comment explaining this is for BullMQ queue

2. Create `src/scoring/scoring.module.ts`:
   - Import { Module } from '@nestjs/common'
   - Import { BullModule } from '@nestjs/bullmq'
   - Import { ConfigModule, ConfigService } from '@nestjs/config'
   - Create @Module with:
     - imports: [
         BullModule.forRootAsync({
           imports: [ConfigModule],
           inject: [ConfigService],
           useFactory: (config: ConfigService) => ({
             connection: {
               url: config.getOrThrow<string>('REDIS_URL'),
             },
           }),
         }),
         BullModule.registerQueue({
           name: 'scoring',
           defaultJobOptions: {
             attempts: 3,
             backoff: { type: 'exponential', delay: 5000 },
             removeOnComplete: 100,
             removeOnFail: 500,
           },
         }),
       ]
     - providers: [] (will be filled in 05-02)
     - exports: [] (will export ScoringService in 05-03)
   - Export ScoringModule class

3. Update `src/app.module.ts`:
   - Import ScoringModule from './scoring/scoring.module.js'
   - Add ScoringModule to imports array

4. Install dependencies if not present:
   ```bash
   npm install @nestjs/bullmq bullmq ioredis
   ```
  </action>
  <verify>
    - Run `npm run build` - should compile without errors
    - Check ScoringModule is imported in AppModule
    - If REDIS_URL not set, startup will fail with validation error (expected)
  </verify>
  <done>
    REDIS_URL environment variable validated.
    ScoringModule created with BullMQ forRootAsync configuration.
    Queue 'scoring' registered with retry settings (3 attempts, exponential backoff).
    ScoringModule imported into AppModule.
  </done>
</task>

<task type="auto">
  <name>Task 3: Push database changes and verify</name>
  <files>
    (database migration)
  </files>
  <action>
1. Run database push:
   ```bash
   npx prisma db push
   ```
   Note: If Windows/OneDrive path issues occur, this may need to be done from WSL or a different path.

2. Generate Prisma client:
   ```bash
   npx prisma generate
   ```

3. Verify table exists using Prisma Studio:
   ```bash
   npx prisma studio
   ```
   - Check that risk_score_results table appears
   - Check that RiskLevel enum column shows in applications table (via relation)

4. Run `npm run build` to ensure everything compiles with new Prisma types.
  </action>
  <verify>
    - `npx prisma db push` completes successfully
    - `npx prisma studio` shows risk_score_results table
    - `npm run build` passes
  </verify>
  <done>
    risk_score_results table exists in Supabase PostgreSQL.
    Prisma client generated with RiskScoreResult type.
    Application now includes optional riskScore relation.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Database model exists:
   ```bash
   npx prisma db pull --print | grep -A 5 "risk_score_results"
   ```

2. TypeScript types available:
   ```typescript
   import { RiskLevel } from './src/common/enums';
   import { RiskScoreResult } from '@prisma/client';
   ```

3. BullMQ configured:
   - ScoringModule imports BullModule.forRootAsync
   - Queue 'scoring' registered

4. Environment validated:
   - REDIS_URL required in env.validation.ts
</verification>

<success_criteria>
- [ ] RiskLevel enum exists in TypeScript AND Prisma schema
- [ ] RiskScoreResult model has all score fields (total, financial, stability, history, integrity)
- [ ] RiskScoreResult has JSON columns for signals, drivers, flags, conditions
- [ ] Application model has riskScore relation
- [ ] risk_score_results table created in database
- [ ] REDIS_URL environment variable validated
- [ ] ScoringModule created with BullMQ configuration
- [ ] Queue 'scoring' registered with retry settings
- [ ] ScoringModule imported in AppModule
- [ ] `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-engine/05-01-SUMMARY.md`
</output>
