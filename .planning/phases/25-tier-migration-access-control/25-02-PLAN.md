---
phase: 25-tier-migration-access-control
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/subscriptions/services/subscriptions.service.ts
  - src/ai/ai.controller.ts
autonomous: true

must_haves:
  truths:
    - "Todas las referencias a SubscriptionPlan.FREE en subscriptions.service.ts usan SubscriptionPlan.STARTER"
    - "El fallback hardcodeado en getUserPlanConfig() retorna tier STARTER"
    - "Los downgrades de suscripcion cambian a STARTER (no FREE)"
    - "La comparacion de plan gratuito en ai.controller.ts usa STARTER"
    - "npm run build compila sin errores"
  artifacts:
    - path: "src/subscriptions/services/subscriptions.service.ts"
      provides: "Updated subscription logic with STARTER/FLEX references"
      contains: "SubscriptionPlan.STARTER"
    - path: "src/ai/ai.controller.ts"
      provides: "Updated AI controller with STARTER tier check"
      contains: "STARTER"
  key_links:
    - from: "src/subscriptions/services/subscriptions.service.ts"
      to: "src/common/enums/subscription-plan.enum.ts"
      via: "Import SubscriptionPlan enum"
      pattern: "SubscriptionPlan\\.STARTER"
    - from: "src/subscriptions/services/subscriptions.service.ts"
      to: "prisma (SubscriptionPlanConfig)"
      via: "getUserPlanConfig fallback"
      pattern: "tier.*SubscriptionPlan\\.STARTER"
---

<objective>
Update all source code references from FREE/BUSINESS to STARTER/FLEX so the existing subscription system works seamlessly with the renamed tiers.

Purpose: After Plan 01 renamed the enum values, all TypeScript code that references the old values must be updated to compile and function correctly. This ensures backward compatibility of all subscription endpoints.
Output: All subscription-related source code uses STARTER/FLEX, application builds cleanly.
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-tier-migration-access-control/25-RESEARCH.md
@.planning/phases/25-tier-migration-access-control/25-01-SUMMARY.md
@src/subscriptions/services/subscriptions.service.ts
@src/ai/ai.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all SubscriptionPlan.FREE references in subscriptions.service.ts</name>
  <files>src/subscriptions/services/subscriptions.service.ts</files>
  <action>
    Update ALL occurrences of `SubscriptionPlan.FREE` to `SubscriptionPlan.STARTER` in `src/subscriptions/services/subscriptions.service.ts`. There are 7 occurrences at the following locations (line numbers from research, verify current lines):

    1. **Line ~105** — fallback in getUserPlanConfig when no active subscription:
       `SubscriptionPlan.FREE` -> `SubscriptionPlan.STARTER`

    2. **Line ~119** — hardcoded fallback object tier:
       `tier: SubscriptionPlan.FREE` -> `tier: SubscriptionPlan.STARTER`

    3. **Line ~261** — free plan check in subscribe():
       `(plan.tier as string) === SubscriptionPlan.FREE` -> `(plan.tier as string) === SubscriptionPlan.STARTER`

    4. **Line ~460** — free plan check in changePlan():
       `(newPlan.tier as string) === SubscriptionPlan.FREE` -> `(newPlan.tier as string) === SubscriptionPlan.STARTER`

    5. **Line ~591** — downgrade subscriptionPlan on user:
       `subscriptionPlan: SubscriptionPlan.FREE` -> `subscriptionPlan: SubscriptionPlan.STARTER`

    6. **Line ~652** — another downgrade subscriptionPlan on user:
       `subscriptionPlan: SubscriptionPlan.FREE` -> `subscriptionPlan: SubscriptionPlan.STARTER`

    7. Verify there are no other FREE/BUSINESS references in the file.

    Also update any user-facing strings in the file that mention "FREE" or "BUSINESS" tier names (e.g., error messages, log messages) to use "Starter" and "Flex" respectively.

    Do NOT change the import statement — `SubscriptionPlan` is already imported from the enum file which was updated in Plan 01.
  </action>
  <verify>
    1. `grep -n "SubscriptionPlan.FREE\|SubscriptionPlan.BUSINESS" src/subscriptions/services/subscriptions.service.ts` returns no matches
    2. `grep -c "SubscriptionPlan.STARTER" src/subscriptions/services/subscriptions.service.ts` returns 6 or more matches
    3. `npx tsc --noEmit` compiles without type errors
  </verify>
  <done>All 7 SubscriptionPlan.FREE references in subscriptions.service.ts changed to SubscriptionPlan.STARTER. No references to old enum values remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update AI controller tier check and verify full build</name>
  <files>src/ai/ai.controller.ts</files>
  <action>
    1. In `src/ai/ai.controller.ts` line ~293, update the `enforceSubscription` method:
       - Change `planConfig.tier === 'FREE'` to `planConfig.tier === 'STARTER'`
       - Update the error message from "requiere plan PRO o BUSINESS" to "requiere plan PRO o FLEX"
       The updated method should be:
       ```typescript
       private async enforceSubscription(userId: string): Promise<void> {
         const planConfig =
           await this.subscriptionsService.getUserPlanConfig(userId);

         if (planConfig.tier === 'STARTER') {
           throw new ForbiddenException(
             'AI Document Analysis requiere plan PRO o FLEX. Actualiza tu suscripcion para acceder a esta funcionalidad.',
           );
         }
       }
       ```

    2. Run a comprehensive search for any remaining FREE/BUSINESS string literals across the entire `src/` directory:
       ```bash
       grep -rn "'FREE'\|\"FREE\"\|'BUSINESS'\|\"BUSINESS\"\|SubscriptionPlan\.FREE\|SubscriptionPlan\.BUSINESS" src/ --include="*.ts"
       ```
       Fix any remaining references found (excluding test files which will be handled if they exist).

    3. Run full build to verify everything compiles:
       ```bash
       npm run build
       ```

    4. Run existing tests to verify no regressions:
       ```bash
       npm test -- --passWithNoTests
       ```
  </action>
  <verify>
    1. `grep -rn "'FREE'\|SubscriptionPlan.FREE\|'BUSINESS'\|SubscriptionPlan.BUSINESS" src/ --include="*.ts"` returns no matches (excluding comments)
    2. `npm run build` succeeds with no errors
    3. `npm test -- --passWithNoTests` passes
  </verify>
  <done>AI controller uses STARTER check. No references to FREE or BUSINESS remain in any source file. Application builds and tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` compiles without errors
2. `grep -rn "SubscriptionPlan\.FREE\|SubscriptionPlan\.BUSINESS" src/ --include="*.ts"` returns zero results
3. `grep -rn "'FREE'" src/ --include="*.ts"` returns zero results (no hardcoded string comparisons)
4. Existing subscription endpoints still work (verified by build + test pass)
</verification>

<success_criteria>
- Zero references to SubscriptionPlan.FREE or SubscriptionPlan.BUSINESS in src/
- Zero hardcoded 'FREE' or 'BUSINESS' string comparisons in src/
- npm run build succeeds
- npm test passes (no regressions)
- getUserPlanConfig() fallback returns STARTER tier
- Downgrade flows set user to STARTER plan
</success_criteria>

<output>
After completion, create `.planning/phases/25-tier-migration-access-control/25-02-SUMMARY.md`
</output>
