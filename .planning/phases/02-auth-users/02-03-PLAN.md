---
phase: 02-auth-users
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/users/users.module.ts
  - src/users/users.service.ts
  - src/users/users.controller.ts
  - src/users/dto/update-profile.dto.ts
  - src/users/dto/switch-role.dto.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "User can view own profile via GET /users/me"
    - "User can update profile (firstName, lastName, phone) via PATCH /users/me"
    - "User with BOTH role can switch active role via PATCH /users/me/role"
    - "Profile endpoints return User object with role and profile data"
  artifacts:
    - path: "src/users/users.controller.ts"
      provides: "Profile REST endpoints"
      exports: ["UsersController"]
    - path: "src/users/users.service.ts"
      provides: "User CRUD operations"
      exports: ["UsersService"]
    - path: "src/users/dto/update-profile.dto.ts"
      provides: "Profile update validation"
      exports: ["UpdateProfileDto"]
  key_links:
    - from: "src/users/users.controller.ts"
      to: "src/auth/decorators/current-user.decorator.ts"
      via: "@CurrentUser() decorator"
      pattern: "@CurrentUser\\(\\)"
    - from: "src/users/users.service.ts"
      to: "prisma.user"
      via: "Prisma client queries"
      pattern: "this\\.prisma\\.user\\."
    - from: "src/app.module.ts"
      to: "src/users/users.module.ts"
      via: "imports array"
      pattern: "UsersModule"
---

<objective>
Implement user profile management endpoints for viewing and updating user data.

Purpose: Allows authenticated users to view and update their profile information. Users with BOTH role can switch between TENANT and LANDLORD contexts.

Output: UsersModule with service and controller providing profile CRUD operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-users/02-RESEARCH.md
@.planning/phases/02-auth-users/02-02-SUMMARY.md

# Existing codebase
@src/app.module.ts
@src/auth/decorators/current-user.decorator.ts
@src/auth/decorators/roles.decorator.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DTOs for profile operations</name>
  <files>src/users/dto/update-profile.dto.ts, src/users/dto/switch-role.dto.ts</files>
  <action>
1. Create src/users/dto/update-profile.dto.ts:
   - firstName: string, optional, @IsString(), @IsOptional(), @MaxLength(50)
   - lastName: string, optional, @IsString(), @IsOptional(), @MaxLength(50)
   - phone: string, optional, @IsString(), @IsOptional(), @Matches() for Colombian phone format
   - Use class-validator decorators
   - Add Swagger decorators (@ApiProperty with required: false)

2. Create src/users/dto/switch-role.dto.ts:
   - activeRole: enum, required, must be TENANT or LANDLORD (not BOTH)
   - Use @IsEnum() with subset [Role.TENANT, Role.LANDLORD]
   - Add Swagger decorators

Colombian phone regex: /^(\+57)?[3][0-9]{9}$/ (mobile) or allow general format.
  </action>
  <verify>
`npm run build` succeeds.
DTOs have proper validation decorators.
  </verify>
  <done>DTOs created with validation for profile update and role switch</done>
</task>

<task type="auto">
  <name>Task 2: Create UsersService with profile operations</name>
  <files>src/users/users.service.ts</files>
  <action>
Create src/users/users.service.ts with:

1. Inject PrismaService

2. findById(id: string): Promise<User>
   - Find user by UUID
   - Throw NotFoundException if not found
   - Return full user object

3. updateProfile(userId: string, dto: UpdateProfileDto): Promise<User>
   - Update firstName, lastName, phone
   - Return updated user

4. setActiveRole(userId: string, activeRole: Role): Promise<User>
   - First verify user has role === BOTH
   - If not BOTH, throw ForbiddenException("Only users with BOTH role can switch")
   - Update activeRole field
   - Return updated user

Use @Injectable() decorator. Import from @prisma/client for User type.
  </action>
  <verify>
`npm run build` succeeds.
Service methods match expected signatures.
  </verify>
  <done>UsersService provides findById, updateProfile, setActiveRole methods</done>
</task>

<task type="auto">
  <name>Task 3: Create UsersController and UsersModule</name>
  <files>src/users/users.controller.ts, src/users/users.module.ts, src/app.module.ts</files>
  <action>
1. Create src/users/users.controller.ts:

   @ApiTags('users')
   @ApiBearerAuth()
   @Controller('users')

   GET /users/me - getProfile()
   - Use @CurrentUser() to get full user from request
   - Return user object directly
   - Add Swagger decorators for 200 response

   PATCH /users/me - updateProfile()
   - Use @CurrentUser('id') to get just the user ID
   - Accept @Body() UpdateProfileDto
   - Call service.updateProfile()
   - Return updated user

   PATCH /users/me/role - switchRole()
   - Use @Roles(Role.BOTH) to restrict to BOTH users only
   - Use @CurrentUser('id') for user ID
   - Accept @Body() SwitchRoleDto
   - Call service.setActiveRole()
   - Return updated user

2. Create src/users/users.module.ts:
   - Import PrismaModule
   - Provide UsersService
   - Declare UsersController
   - Export UsersService (needed by auth strategy)

3. Update src/app.module.ts:
   - Import UsersModule

4. Update src/auth/auth.module.ts:
   - Import UsersModule
   - Strategy needs UsersService to lookup users

IMPORTANT: The auth strategy (02-02) already imports UsersService. This task creates the actual service. The auth module should forwardRef to UsersModule to avoid circular dependency, OR UsersModule should be imported before AuthModule.
  </action>
  <verify>
`npm run build` succeeds.
`npm run start:dev` starts without circular dependency errors.
Routes visible in Swagger at /api.
  </verify>
  <done>Users endpoints operational: GET /users/me, PATCH /users/me, PATCH /users/me/role</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npm run build` succeeds
2. `npm run start:dev` starts successfully
3. Swagger shows /users/me endpoints with auth required
4. GET /users/me with valid JWT returns user profile
5. PATCH /users/me with valid JWT updates profile
6. PATCH /users/me/role with BOTH user switches role
7. PATCH /users/me/role with TENANT/LANDLORD user returns 403
</verification>

<success_criteria>
- GET /users/me returns authenticated user's profile
- PATCH /users/me updates firstName, lastName, phone with validation
- PATCH /users/me/role allows BOTH users to switch active role
- Non-BOTH users get 403 when trying to switch role
- All endpoints require valid JWT (401 without)
- Swagger documents all endpoints with schemas
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-users/02-03-SUMMARY.md`
</output>
