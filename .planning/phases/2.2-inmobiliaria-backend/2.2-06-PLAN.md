---
phase: 2.2-inmobiliaria-backend
plan: 06
type: execute
wave: 4
depends_on: ["2.2-03"]
files_modified:
  - src/inmobiliaria/mantenimiento/mantenimiento.module.ts
  - src/inmobiliaria/mantenimiento/mantenimiento.service.ts
  - src/inmobiliaria/mantenimiento/mantenimiento.controller.ts
  - src/inmobiliaria/mantenimiento/dto/create-mantenimiento.dto.ts
  - src/inmobiliaria/mantenimiento/dto/add-quote.dto.ts
  - src/inmobiliaria/mantenimiento/dto/index.ts
  - src/inmobiliaria/renovaciones/renovaciones.module.ts
  - src/inmobiliaria/renovaciones/renovaciones.service.ts
  - src/inmobiliaria/renovaciones/renovaciones.controller.ts
  - src/inmobiliaria/renovaciones/dto/create-renovacion.dto.ts
  - src/inmobiliaria/renovaciones/dto/update-stage.dto.ts
  - src/inmobiliaria/renovaciones/dto/index.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Maintenance requests with multi-quote system: create, list, add quote, select quote, approve, complete, cancel"
    - "Maintenance auto-fills propietarioId and names from consignacion"
    - "Maintenance status transitions: REPORTED -> QUOTED -> MAINT_APPROVED -> IN_PROGRESS -> MAINT_COMPLETED"
    - "Lease renewal creation with auto-calculated daysUntilExpiry and IPC fields"
    - "Renewal status transitions with timestamp tracking: RENOV_PENDING -> NOTIFIED -> NEGOTIATING -> RENOV_APPROVED -> RENOV_SIGNED -> RENOV_COMPLETED"
    - "Upcoming renewals endpoint returns expiring leases within N days"
    - "Renewal history audit trail (RenovacionHistory entries)"
    - "All endpoints scoped to agency"
  artifacts:
    - path: "src/inmobiliaria/mantenimiento/mantenimiento.module.ts"
      provides: "MantenimientoModule"
      exports: ["MantenimientoModule"]
    - path: "src/inmobiliaria/renovaciones/renovaciones.module.ts"
      provides: "RenovacionesModule"
      exports: ["RenovacionesModule"]
  key_links:
    - from: "src/inmobiliaria/mantenimiento/mantenimiento.service.ts"
      to: "prisma.solicitudMantenimiento"
      via: "PrismaService"
      pattern: "prisma\\.solicitudMantenimiento"
    - from: "src/inmobiliaria/renovaciones/renovaciones.service.ts"
      to: "prisma.renovacion"
      via: "PrismaService"
      pattern: "prisma\\.renovacion"
---

<objective>
Register and verify the Mantenimiento and Renovaciones modules for maintenance request management and lease renewal workflows.

Purpose: Both modules already exist with complete service implementations. MantenimientoService handles the full maintenance lifecycle (create, quote, select, approve, complete, cancel). RenovacionesService handles renewal detection with IPC-based rent adjustments and audit trail. This plan registers them, fixes controllers and DTOs, and verifies all endpoints.

Output: Working MantenimientoModule (8 endpoints) and RenovacionesModule (5 endpoints).
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-03-SUMMARY.md
@src/inmobiliaria/mantenimiento/mantenimiento.service.ts
@src/inmobiliaria/renovaciones/renovaciones.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and fix MantenimientoModule with controller and DTOs</name>
  <files>src/inmobiliaria/mantenimiento/mantenimiento.module.ts, src/inmobiliaria/mantenimiento/mantenimiento.controller.ts, src/inmobiliaria/mantenimiento/dto/create-mantenimiento.dto.ts, src/inmobiliaria/mantenimiento/dto/add-quote.dto.ts, src/inmobiliaria/mantenimiento/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register MantenimientoModule in AppModule**

    2. **Fix MantenimientoModule**: Declare controller and service. Export service (for ConsignacionesController.getMaintenanceRequests).

    3. **Fix/create MantenimientoController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/maintenance')`
       - `@ApiTags('inmobiliaria/maintenance')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with @Query: status?, priority?, type?, consignacionId?
         - GET /:id - detail with all quotes
         - POST / - create request
         - PUT /:id - update fields
         - POST /:id/quote - add vendor quote
         - PUT /:id/select-quote - select a quote with @Body { quoteId: string }
         - PUT /:id/approve - approve (sets MAINT_APPROVED)
         - PUT /:id/complete - complete with optional @Body { completionNotes?, completionPhotoUrls? }
         - PUT /:id/cancel - cancel (sets MAINT_CANCELLED)

    4. **Fix DTOs**:
       - `CreateMantenimientoDto`: consignacionId!: string (@IsUUID), agenteUserId?: string (@IsOptional, @IsUUID), type!: MantenimientoType (@IsEnum), priority?: MantenimientoPriority (@IsOptional, @IsEnum, default MEDIUM), title!: string (@IsString, @MaxLength(200)), description!: string (@IsString), photoUrls?: string[] (@IsOptional, @IsArray), paidBy?: MantenimientoPaidBy (@IsOptional, @IsEnum, default OWNER)
       - `AddQuoteDto`: providerName!: string (@IsString), providerPhone?: string (@IsOptional, @IsString), amount!: number (@IsInt, @Min(0)), description!: string (@IsString), estimatedDays!: number (@IsInt, @Min(1))
       - Import enums: MantenimientoType, MantenimientoPriority, MantenimientoPaidBy from `../../../common/enums/`

    5. **Verify MantenimientoService**: The existing service correctly:
       - Auto-fills propietarioId, propertyTitle, tenantName, propietarioName from consignacion
       - Transitions to QUOTED on first quote
       - selectQuote sets selectedQuoteId and approvedAmount
       - approve/complete/cancel set correct status values
       - Uses MantenimientoStatus enum for all status transitions
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 8-9 maintenance endpoints
  </verify>
  <done>
    MantenimientoModule registered and working: full maintenance lifecycle from report through quotes, approval, and completion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register and fix RenovacionesModule with controller and DTOs</name>
  <files>src/inmobiliaria/renovaciones/renovaciones.module.ts, src/inmobiliaria/renovaciones/renovaciones.controller.ts, src/inmobiliaria/renovaciones/dto/create-renovacion.dto.ts, src/inmobiliaria/renovaciones/dto/update-stage.dto.ts, src/inmobiliaria/renovaciones/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register RenovacionesModule in AppModule**

    2. **Fix RenovacionesModule**: Declare controller and service.

    3. **Fix/create RenovacionesController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/renovaciones')`
       - `@ApiTags('inmobiliaria/renovaciones')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with @Query: status?, urgency? ('0-30', '31-60', '61-90', '90+')
         - GET /upcoming - upcoming renewals with @Query: days (default 90)
         - GET /:id - detail with history
         - POST / - create renewal
         - PUT /:id/stage - update stage/status with UpdateRenovacionStageDto
       - Place /upcoming route BEFORE /:id

    4. **Fix DTOs**:
       - `CreateRenovacionDto`: consignacionId!: string (@IsUUID), leaseId?: string (@IsOptional, @IsUUID), agenteUserId?: string (@IsOptional, @IsUUID)
       - `UpdateRenovacionStageDto` (or `UpdateStageDto`): status!: RenovacionStatus (@IsEnum), ipcRate?: number (@IsOptional, @IsNumber), proposedRent?: number (@IsOptional, @IsInt), negotiatedRent?: number (@IsOptional, @IsInt), historyNote?: string (@IsOptional, @IsString)
       - Import RenovacionStatus from `../../../common/enums/renovacion-status.enum.js`

    5. **Verify RenovacionesService**: The existing service correctly:
       - Auto-calculates daysUntilExpiry from consignacion.leaseEndDate
       - Updates timestamps based on status (notifiedAt, approvedAt, signedAt, completedAt)
       - Creates history entries on stage transitions
       - Filters by urgency bucket
       - getUpcoming excludes completed/terminated
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 5 renovaciones endpoints
  </verify>
  <done>
    RenovacionesModule registered and working: renewal lifecycle with IPC calculation, urgency buckets, and audit trail.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Swagger shows 13-14 endpoints total: 8-9 maintenance + 5 renovaciones
- Maintenance request lifecycle: REPORTED -> QUOTED -> MAINT_APPROVED -> IN_PROGRESS -> MAINT_COMPLETED
- Renewal status transitions tracked with timestamps and history
- Upcoming renewals sorted by urgency
</verification>

<success_criteria>
MantenimientoModule and RenovacionesModule fully operational. Maintenance supports multi-quote workflow. Renewals support IPC-based negotiation with full audit trail.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-06-SUMMARY.md`
</output>
