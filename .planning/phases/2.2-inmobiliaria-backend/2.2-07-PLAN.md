---
phase: 2.2-inmobiliaria-backend
plan: 07
type: execute
wave: 5
depends_on: ["2.2-03"]
files_modified:
  - src/inmobiliaria/documentos/documentos.module.ts
  - src/inmobiliaria/documentos/documentos.service.ts
  - src/inmobiliaria/documentos/documentos.controller.ts
  - src/inmobiliaria/documentos/dto/create-template.dto.ts
  - src/inmobiliaria/documentos/dto/generate-document.dto.ts
  - src/inmobiliaria/documentos/dto/sign-document.dto.ts
  - src/inmobiliaria/documentos/dto/index.ts
  - src/inmobiliaria/actas/actas.module.ts
  - src/inmobiliaria/actas/actas.service.ts
  - src/inmobiliaria/actas/actas.controller.ts
  - src/inmobiliaria/actas/dto/create-acta.dto.ts
  - src/inmobiliaria/actas/dto/update-acta.dto.ts
  - src/inmobiliaria/actas/dto/sign-acta.dto.ts
  - src/inmobiliaria/actas/dto/index.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Document templates CRUD: create, list (filter by category), get, update, soft-delete"
    - "Document generation from template with variable substitution"
    - "Document signing with signature tracking (JSON array in signatures field)"
    - "Actas CRUD: create, list (filter by type/status/consignacion), get detail, update"
    - "Actas auto-fill propietarioName from propietario lookup"
    - "Actas status transitions: ACTA_DRAFT -> ACTA_IN_PROGRESS -> PENDING_SIGNATURES -> ACTA_COMPLETED"
    - "Actas support inventory items, meter readings, keys, deductions (all as JSON fields)"
    - "Actas signing: first signature -> PENDING_SIGNATURES, second -> ACTA_COMPLETED"
    - "All endpoints scoped to agency"
  artifacts:
    - path: "src/inmobiliaria/documentos/documentos.module.ts"
      provides: "DocumentosModule for agency document management"
      exports: ["DocumentosModule"]
    - path: "src/inmobiliaria/actas/actas.module.ts"
      provides: "ActasModule for property delivery/return acts"
      exports: ["ActasModule"]
  key_links:
    - from: "src/inmobiliaria/documentos/documentos.service.ts"
      to: "prisma.agencyDocumentTemplate"
      via: "PrismaService"
      pattern: "prisma\\.agency(DocumentTemplate|Document)"
    - from: "src/inmobiliaria/actas/actas.service.ts"
      to: "prisma.actaEntrega"
      via: "PrismaService"
      pattern: "prisma\\.actaEntrega"
---

<objective>
Register and verify the Documentos and Actas modules for agency document template management, document generation, and property delivery/return acts.

Purpose: Both modules already exist with complete service implementations. DocumentosService handles template CRUD, document generation from templates, and document signing. ActasService handles acta creation with auto-filled data, inventory/meter/key tracking via JSON fields, and multi-party signing workflow. This plan registers them, fixes controllers and DTOs, and verifies all endpoints.

Output: Working DocumentosModule (5+ endpoints) and ActasModule (5+ endpoints).
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-03-SUMMARY.md
@src/inmobiliaria/documentos/documentos.service.ts
@src/inmobiliaria/actas/actas.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and fix DocumentosModule with controller and DTOs</name>
  <files>src/inmobiliaria/documentos/documentos.module.ts, src/inmobiliaria/documentos/documentos.controller.ts, src/inmobiliaria/documentos/dto/create-template.dto.ts, src/inmobiliaria/documentos/dto/generate-document.dto.ts, src/inmobiliaria/documentos/dto/sign-document.dto.ts, src/inmobiliaria/documentos/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register DocumentosModule in AppModule** (name it `InmobiliariaDocumentosModule` to avoid confusion with existing `DocumentsModule` from Phase 4). Actually, since the existing module file is named `documentos.module.ts`, keep the class name as `DocumentosModule` but ensure no naming collision in AppModule imports. The existing `DocumentsModule` (Phase 4) is at `src/documents/documents.module.ts` and handles application documents - completely separate.

    2. **Fix DocumentosModule**: Declare controller and service.

    3. **Fix/verify DocumentosController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/documents')` (NOT 'inmobiliaria/documentos' - use English for URL path consistency with research doc)
       - `@ApiTags('inmobiliaria/documents')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET /templates - list templates with @Query: category?
         - GET /templates/:id - get template detail
         - POST /templates - create template (admin only)
         - PUT /templates/:id - update template (admin only)
         - DELETE /templates/:id - soft-delete template (admin only)
         - GET / - list generated documents with @Query: consignacionId?, status?
         - GET /:id - get document detail
         - POST /generate - generate document from template
         - POST /:id/sign - sign document
       - Place /templates/* routes BEFORE / and /:id routes to avoid conflicts

    4. **Fix DTOs**:
       - `CreateTemplateDto`: name!: string (@IsString), category!: DocumentTemplateCategory (@IsEnum), content!: string (@IsString), version?: string (@IsOptional, @IsString), variables?: string[] (@IsOptional, @IsArray)
       - `GenerateDocumentDto`: name!: string (@IsString), templateId?: string (@IsOptional, @IsUUID), consignacionId?: string (@IsOptional, @IsUUID), content?: string (@IsOptional, @IsString - used if no template)
       - `SignDocumentDto`: signerName!: string (@IsString), signerEmail!: string (@IsEmail)
       - Import DocumentTemplateCategory from enum

    5. **Verify DocumentosService**: Service correctly handles template CRUD, document generation, and signing with JSON array tracking. When 2+ signatures, status changes to DOC_SIGNED.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 9 documents endpoints
  </verify>
  <done>
    DocumentosModule registered and working: template CRUD, document generation, signing workflow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register and fix ActasModule with controller and DTOs</name>
  <files>src/inmobiliaria/actas/actas.module.ts, src/inmobiliaria/actas/actas.controller.ts, src/inmobiliaria/actas/dto/create-acta.dto.ts, src/inmobiliaria/actas/dto/update-acta.dto.ts, src/inmobiliaria/actas/dto/sign-acta.dto.ts, src/inmobiliaria/actas/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register ActasModule in AppModule**

    2. **Fix ActasModule**: Declare controller and service.

    3. **Fix/create ActasController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/actas')`
       - `@ApiTags('inmobiliaria/actas')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with @Query: type? (entrega/devolucion), status?, consignacionId?
         - GET /:id - detail with consignacion and propietario includes
         - POST / - create acta
         - PUT /:id - update acta (inventory, meters, keys, etc.)
         - POST /:id/sign - sign acta
         - DELETE /:id - delete (only if ACTA_DRAFT)

    4. **Fix DTOs**:
       - `CreateActaDto`: type!: ActaType (@IsEnum), propietarioId!: string (@IsUUID), consignacionId?: string (@IsOptional, @IsUUID), leaseId?: string (@IsOptional, @IsUUID), agenteUserId?: string (@IsOptional, @IsUUID), propertyTitle!: string (@IsString), propertyAddress!: string (@IsString), tenantName?: string (@IsOptional, @IsString), generalCondition?: ItemCondition (@IsOptional, @IsEnum), generalObservations?: string (@IsOptional, @IsString), depositAmount?: number (@IsOptional, @IsInt)
       - `UpdateActaDto`: All fields optional. PLUS JSON fields: rooms?: any[] (@IsOptional, @IsArray), items?: any[] (@IsOptional, @IsArray), meterReadings?: any[] (@IsOptional, @IsArray), keysDelivered?: any[] (@IsOptional, @IsArray), deductions?: any[] (@IsOptional, @IsArray), depositToReturn?: number (@IsOptional, @IsInt), photoUrls?: string[] (@IsOptional, @IsArray). Also: type?, consignacionId?, leaseId?, propietarioId?, agenteUserId?, propertyTitle?, propertyAddress?, tenantName?, generalCondition?, generalObservations?, depositAmount?
       - `SignActaDto`: signerName!: string (@IsString), signerEmail!: string (@IsEmail), signerRole?: string (@IsOptional, @IsString)
       - Import ActaType, ItemCondition from enums

    5. **Verify ActasService**: Service correctly:
       - Auto-fills propietarioName from propietario lookup
       - Transitions from ACTA_DRAFT to ACTA_IN_PROGRESS when rooms/items are set
       - Signing: first signature -> PENDING_SIGNATURES, 2+ signatures -> ACTA_COMPLETED
       - Delete only allowed for ACTA_DRAFT status
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 6 actas endpoints
  </verify>
  <done>
    ActasModule registered and working: property delivery/return acts with inventory, meters, keys, signatures, and deductions.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Swagger shows 15 endpoints total: 9 documents + 6 actas
- Template CRUD works
- Document generation from template works
- Acta lifecycle: create -> add inventory/meters -> sign -> complete
</verification>

<success_criteria>
DocumentosModule and ActasModule fully operational. Templates support variable substitution. Actas support full inventory tracking with multi-party signing.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-07-SUMMARY.md`
</output>
