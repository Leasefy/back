---
phase: 2.2-inmobiliaria-backend
plan: 08
type: execute
wave: 6
depends_on: ["2.2-03", "2.2-04", "2.2-05", "2.2-06", "2.2-07"]
files_modified:
  - src/inmobiliaria/reports/reports.module.ts
  - src/inmobiliaria/reports/reports.controller.ts
  - src/inmobiliaria/reports/reports.service.ts
  - src/inmobiliaria/analytics/analytics.module.ts
  - src/inmobiliaria/analytics/analytics.controller.ts
  - src/inmobiliaria/analytics/analytics.service.ts
  - src/inmobiliaria/dashboard/dashboard.module.ts
  - src/inmobiliaria/dashboard/dashboard.controller.ts
  - src/inmobiliaria/dashboard/dashboard.service.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "7 report endpoints: extracto-propietario, cartera, comisiones, ocupacion, vencimientos, flujo-caja, rendimiento-agentes"
    - "Reports use AgencyMemberGuard + @CurrentAgency instead of manual resolveAgencyId pattern"
    - "Analytics: 4 endpoints - kpis (8 KPIs), charts (4 chart datasets), trends (12-month metric), forecast (linear projection)"
    - "Dashboard: 3 endpoints - kpis (aggregated), quick-stats (urgent counts), activity (mixed feed)"
    - "All endpoints scoped to agency"
    - "All three modules registered in AppModule"
  artifacts:
    - path: "src/inmobiliaria/reports/reports.module.ts"
      provides: "ReportsModule for 7 report types"
      exports: ["ReportsModule"]
    - path: "src/inmobiliaria/analytics/analytics.module.ts"
      provides: "AnalyticsModule for KPIs, charts, trends, forecasts"
      exports: ["AnalyticsModule"]
    - path: "src/inmobiliaria/dashboard/dashboard.module.ts"
      provides: "DashboardModule (inmobiliaria) for agency dashboard"
      exports: ["InmobiliariaDashboardModule"]
  key_links:
    - from: "src/inmobiliaria/reports/reports.service.ts"
      to: "prisma.cobro, prisma.consignacion, prisma.propietario, prisma.pipelineItem, prisma.dispersion"
      via: "PrismaService"
      pattern: "prisma\\.(cobro|consignacion|propietario|pipelineItem|dispersion|agencyMember)"
    - from: "src/inmobiliaria/analytics/analytics.service.ts"
      to: "prisma.cobro, prisma.consignacion, prisma.pipelineItem, prisma.renovacion"
      via: "PrismaService"
      pattern: "prisma\\.(cobro|consignacion|pipelineItem|renovacion|propietario)"
    - from: "src/inmobiliaria/dashboard/dashboard.service.ts"
      to: "prisma.consignacion, prisma.cobro, prisma.pipelineItem, prisma.dispersion, prisma.solicitudMantenimiento, prisma.actaEntrega, prisma.renovacion, prisma.agencyMember"
      via: "PrismaService"
      pattern: "prisma\\.(consignacion|cobro|pipelineItem|dispersion|solicitudMantenimiento|actaEntrega|renovacion|agencyMember|propietario)"
---

<objective>
Register and verify the Reports, Analytics, and Dashboard modules for agency reporting, KPI tracking, trend analysis, forecasting, and dashboard aggregation.

Purpose: All three modules already exist with complete service implementations. ReportsService has 7 report types (extracto, cartera, comisiones, ocupacion, vencimientos, flujo-caja, rendimiento). AnalyticsService provides KPIs, chart datasets, 12-month trends, and 3-month linear forecasts. DashboardService aggregates urgent counts, pipeline KPIs, and a mixed activity feed. However, the controllers use a legacy pattern (manual resolveAgencyId via AgencyService injection) instead of the standardized AgencyMemberGuard + @CurrentAgency decorator used by all other inmobiliaria modules. This plan registers them, converts controllers to use the guard pattern for consistency, and verifies compilation.

IMPORTANT: The existing `DashboardModule` must be renamed to `InmobiliariaDashboardModule` (or use a distinct class name) to avoid collision with the existing Phase 18 dashboard at `src/dashboard/`. The Phase 18 module handles tenant/landlord dashboards; this module handles agency (inmobiliaria) dashboards.

Output: Working ReportsModule (7 endpoints), AnalyticsModule (4 endpoints), and InmobiliariaDashboardModule (3 endpoints).
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-03-SUMMARY.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-05-SUMMARY.md
@src/inmobiliaria/reports/reports.service.ts
@src/inmobiliaria/reports/reports.controller.ts
@src/inmobiliaria/analytics/analytics.service.ts
@src/inmobiliaria/analytics/analytics.controller.ts
@src/inmobiliaria/dashboard/dashboard.service.ts
@src/inmobiliaria/dashboard/dashboard.controller.ts
@src/inmobiliaria/agency/guards/agency-member.guard.ts
@src/inmobiliaria/agency/decorators/current-agency.decorator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and fix ReportsModule with AgencyMemberGuard</name>
  <files>src/inmobiliaria/reports/reports.module.ts, src/inmobiliaria/reports/reports.controller.ts, src/app.module.ts</files>
  <action>
    1. **Register ReportsModule in AppModule**: Add import and include in imports array. Name note: This is the inmobiliaria ReportsModule at `src/inmobiliaria/reports/reports.module.ts` - no naming collision exists since there's no Phase-based ReportsModule elsewhere.

    2. **Fix ReportsModule**: Currently imports `AgencyModule` (for AgencyService). After converting the controller to use AgencyMemberGuard, the AgencyModule import is still needed (it exports AgencyMemberGuard and the decorator). Verify controller and service are declared.

    3. **Convert ReportsController to use AgencyMemberGuard + @CurrentAgency**:
       - Remove `AgencyService` injection from constructor
       - Remove the `resolveAgencyId` private method
       - Add `@UseGuards(AgencyMemberGuard)` at class level
       - Replace `@CurrentUser('id') userId: string` + `resolveAgencyId(userId)` pattern with `@CurrentAgency('agencyId') agencyId: string`
       - Import `UseGuards` from `@nestjs/common`
       - Import `AgencyMemberGuard` from `../agency/guards/agency-member.guard.js`
       - Import `CurrentAgency` from `../agency/decorators/current-agency.decorator.js`
       - Remove `CurrentUser` import
       - All 7 endpoint handlers become simpler (one less async call per handler)
       - Endpoints remain the same:
         - GET /extracto/:propietarioId?month=YYYY-MM
         - GET /cartera
         - GET /comisiones?month=YYYY-MM
         - GET /ocupacion
         - GET /vencimientos
         - GET /flujo-caja?period=&months=
         - GET /rendimiento-agentes?month=YYYY-MM

    4. **Verify ReportsService**: Service is correct. All 7 methods take agencyId as first parameter. Uses proper enum values (CobroStatus.PAID, DispersionStatus.DISP_COMPLETED, etc.). No field name bugs.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 7 report endpoints under `inmobiliaria/reports`
  </verify>
  <done>
    ReportsModule registered and working: 7 report types with AgencyMemberGuard authorization.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register and fix Analytics + Dashboard modules with AgencyMemberGuard</name>
  <files>src/inmobiliaria/analytics/analytics.module.ts, src/inmobiliaria/analytics/analytics.controller.ts, src/inmobiliaria/dashboard/dashboard.module.ts, src/inmobiliaria/dashboard/dashboard.controller.ts, src/app.module.ts</files>
  <action>
    1. **Register AnalyticsModule in AppModule**

    2. **Convert AnalyticsController to AgencyMemberGuard pattern** (same refactor as Task 1):
       - Remove `AgencyService` injection and `resolveAgencyId` method
       - Add `@UseGuards(AgencyMemberGuard)` at class level
       - Replace all `@CurrentUser('id') userId` + resolveAgencyId with `@CurrentAgency('agencyId') agencyId: string`
       - Endpoints remain:
         - GET /kpis - returns array of 8 KPI objects
         - GET /charts - returns 4 chart datasets (revenueByMonth, occupancyByZone, collectionStatus, pipelineByStage)
         - GET /trends/:metricId - 12-month data points
         - GET /forecast/:metricId - 6 months historical + 3 months projected

    3. **Verify AnalyticsService**: Service is correct. getKpis returns 8 KPIs. getCharts returns 4 datasets. getTrend returns 12 data points. getForecast uses linear regression. All use proper enum values.

    4. **Rename DashboardModule class** to `InmobiliariaDashboardModule` to avoid collision with existing Phase 18 `DashboardModule` at `src/dashboard/dashboard.module.ts`. Update the module file:
       - `export class InmobiliariaDashboardModule {}`
       - Keep the same file path

    5. **Register InmobiliariaDashboardModule in AppModule**

    6. **Convert DashboardController to AgencyMemberGuard pattern** (same refactor):
       - Remove `AgencyService` injection and `resolveAgencyId` method
       - Add `@UseGuards(AgencyMemberGuard)` at class level
       - Replace all `@CurrentUser('id') userId` + resolveAgencyId with `@CurrentAgency('agencyId') agencyId: string`
       - Endpoints remain:
         - GET /kpis - aggregated dashboard KPIs (19 metrics: properties, revenue, pipeline, agents)
         - GET /quick-stats - urgent counts (maintenance, signatures, expirations, overdue)
         - GET /activity?limit=10 - mixed activity feed sorted by date

    7. **Verify DashboardService**: Service is correct. getDashboardKpis aggregates data from consignaciones, cobros, pipeline, dispersiones, agencyMembers, propietarios. getQuickStats queries solicitudMantenimiento, actaEntrega, renovacion, cobro for urgent items. getRecentActivity normalizes 3 entity types into a unified feed.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 4 analytics endpoints under `inmobiliaria/analytics`
    - Swagger shows 3 dashboard endpoints under `inmobiliaria/dashboard`
    - No naming conflict with Phase 18 DashboardModule
  </verify>
  <done>
    AnalyticsModule and InmobiliariaDashboardModule registered and working: KPIs, charts, trends, forecasts, quick stats, and activity feed. All using AgencyMemberGuard for consistent authorization.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Swagger shows 14 endpoints total: 7 reports + 4 analytics + 3 dashboard
- All 3 modules use AgencyMemberGuard (consistent with all other inmobiliaria modules)
- Report data aggregation works correctly across cobros, dispersiones, consignaciones, pipeline
- Analytics KPIs return 8 metric objects with category classification
- Dashboard quick-stats returns 4 urgent count metrics
- Activity feed returns mixed items from cobros, pipeline, and mantenimiento
</verification>

<success_criteria>
ReportsModule, AnalyticsModule, and InmobiliariaDashboardModule fully operational. 7 report types cover all financial, operational, and performance reporting needs. Analytics provides KPIs, charts, trends, and linear forecasts. Dashboard provides at-a-glance agency health with urgent item alerts. All endpoints authorized via AgencyMemberGuard for consistent multi-tenant scoping.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-08-SUMMARY.md`
</output>
