---
phase: 2.2-inmobiliaria-backend
plan: 04
type: execute
wave: 3
depends_on: ["2.2-02"]
files_modified:
  - src/inmobiliaria/pipeline/pipeline.module.ts
  - src/inmobiliaria/pipeline/pipeline.service.ts
  - src/inmobiliaria/pipeline/pipeline.controller.ts
  - src/inmobiliaria/pipeline/dto/create-pipeline-item.dto.ts
  - src/inmobiliaria/pipeline/dto/update-pipeline-item.dto.ts
  - src/inmobiliaria/pipeline/dto/move-stage.dto.ts
  - src/inmobiliaria/pipeline/dto/log-action.dto.ts
  - src/inmobiliaria/pipeline/dto/index.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Pipeline CRUD works: create, list (with filters by stage/agent/search), get detail, update"
    - "Stage transitions work: move item between 10 stages (LEAD -> COMPLETED or LOST)"
    - "Action logging works: records contact actions with timestamps and notes"
    - "Pipeline stats work: counts per stage, total active, conversion rate"
    - "All endpoints scoped to agency via AgencyMemberGuard"
    - "Kanban-ready API: list returns items that can be grouped by stage on frontend"
  artifacts:
    - path: "src/inmobiliaria/pipeline/pipeline.module.ts"
      provides: "PipelineModule"
      exports: ["PipelineModule"]
    - path: "src/inmobiliaria/pipeline/pipeline.service.ts"
      provides: "PipelineService with CRUD + stage transitions + stats"
      exports: ["PipelineService"]
  key_links:
    - from: "src/inmobiliaria/pipeline/pipeline.controller.ts"
      to: "src/inmobiliaria/agency/guards/agency-member.guard.ts"
      via: "@UseGuards(AgencyMemberGuard)"
      pattern: "AgencyMemberGuard"
---

<objective>
Register and fix the Pipeline module for 10-stage sales funnel tracking with Kanban-ready API.

Purpose: The PipelineModule already exists but has a critical bug: the `CONSIGNACION_INCLUDE` constant references non-existent fields (`direccion`, `ciudad`, `barrio`, `tipoInmueble`, `canonMensual`). These need to be fixed to match the actual Consignacion schema fields (`propertyTitle`, `propertyAddress`, `propertyCity`, `propertyType`, `monthlyRent`). The module also needs to be registered in AppModule.

Output: Working PipelineModule with 7 endpoints for pipeline management.
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-02-SUMMARY.md
@src/inmobiliaria/pipeline/pipeline.service.ts
@src/inmobiliaria/pipeline/pipeline.controller.ts
@prisma/schema.prisma (Consignacion model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PipelineService bugs and register module</name>
  <files>src/inmobiliaria/pipeline/pipeline.service.ts, src/inmobiliaria/pipeline/pipeline.module.ts, src/app.module.ts</files>
  <action>
    1. **CRITICAL FIX in PipelineService**: The `CONSIGNACION_INCLUDE` constant at the top of the file references WRONG field names. Fix it:
       ```
       // WRONG:
       const CONSIGNACION_INCLUDE = {
         consignacion: {
           select: { id: true, direccion: true, ciudad: true, barrio: true, tipoInmueble: true, canonMensual: true }
         }
       };

       // CORRECT:
       const CONSIGNACION_INCLUDE = {
         consignacion: {
           select: { id: true, propertyTitle: true, propertyAddress: true, propertyCity: true, propertyType: true, monthlyRent: true }
         }
       };
       ```

    2. **Register PipelineModule in AppModule**: Add import and add to imports array.

    3. **Verify PipelineModule**: Should declare `PipelineController` and `PipelineService`.

    4. **Verify service methods**: The existing service has all 7 operations:
       - `create(agencyId, dto)` - creates pipeline item
       - `findAll(agencyId, query)` - lists with filters (stage, agent, search, consignacion)
       - `findOne(agencyId, id)` - get detail
       - `update(agencyId, id, dto)` - update fields
       - `moveStage(agencyId, id, dto)` - transition stage, calculates daysInStage
       - `logAction(agencyId, id, dto)` - log contact with timestamp
       - `getStats(agencyId)` - counts per stage, conversion rate
       - `remove(agencyId, id)` - delete

    5. **Verify all imports** use `.js` extension for ESM compatibility.
  </action>
  <verify>
    - `npx tsc --noEmit` passes (no more references to non-existent fields)
    - Server starts
  </verify>
  <done>
    PipelineService bug fixed (correct Consignacion field names). PipelineModule registered in AppModule.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix PipelineController and DTOs</name>
  <files>src/inmobiliaria/pipeline/pipeline.controller.ts, src/inmobiliaria/pipeline/dto/create-pipeline-item.dto.ts, src/inmobiliaria/pipeline/dto/update-pipeline-item.dto.ts, src/inmobiliaria/pipeline/dto/move-stage.dto.ts, src/inmobiliaria/pipeline/dto/log-action.dto.ts, src/inmobiliaria/pipeline/dto/index.ts</files>
  <action>
    1. **Fix PipelineController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/pipeline')`
       - `@ApiTags('inmobiliaria/pipeline')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with @Query: stage?, agenteUserId?, search?, consignacionId?
         - GET /stats - pipeline statistics (must be BEFORE /:id route to avoid conflict)
         - GET /:id - detail
         - POST / - create
         - PUT /:id - update
         - PUT /:id/stage - move stage
         - POST /:id/action - log action
         - DELETE /:id - delete pipeline item

    2. **Fix DTOs with class-validator**:
       - `CreatePipelineItemDto`: consignacionId!: string (@IsUUID), agenteUserId?: string (@IsOptional, @IsUUID), candidateName!: string (@IsString), candidateEmail?: string (@IsOptional, @IsEmail), candidatePhone?: string (@IsOptional, @IsString), candidateAvatar?: string, riskScore?: number (@IsOptional, @IsInt), riskLevel?: string (@IsOptional, @IsString), stage?: PipelineStage (@IsOptional, @IsEnum(PipelineStage)), nextAction?: string, nextActionDate?: string (@IsOptional, @IsDateString), notes?: string
       - `UpdatePipelineItemDto`: All fields optional
       - `MoveStageDto`: stage!: PipelineStage (@IsEnum(PipelineStage)), lostReason?: string (@IsOptional, @IsString - only for LOST stage)
       - `LogActionDto`: action!: string (@IsString), notes?: string (@IsOptional, @IsString), nextActionDate?: string (@IsOptional, @IsDateString)
       - Import PipelineStage from `../../../common/enums/pipeline-stage.enum.js`

    3. **Ensure stats route doesn't conflict with /:id**: Place `@Get('stats')` handler BEFORE `@Get(':id')` in the controller class.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 7-8 pipeline endpoints
    - GET /inmobiliaria/pipeline/stats route is accessible (not caught by /:id)
  </verify>
  <done>
    PipelineController working with 7 endpoints. All DTOs validated. Stats route accessible. Kanban-ready API operational.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Swagger shows 7-8 pipeline endpoints under `inmobiliaria/pipeline`
- Pipeline items can be created, listed, moved between stages, and deleted
- Stats endpoint returns stage counts and conversion rate
</verification>

<success_criteria>
PipelineModule fully operational: 10-stage funnel tracking, stage transitions with daysInStage calculation, action logging, statistics. Kanban-ready API that returns items groupable by stage.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-04-SUMMARY.md`
</output>
