---
phase: 2.2-inmobiliaria-backend
plan: 05
type: execute
wave: 4
depends_on: ["2.2-03"]
files_modified:
  - src/inmobiliaria/cobros/cobros.module.ts
  - src/inmobiliaria/cobros/cobros.service.ts
  - src/inmobiliaria/cobros/cobros.controller.ts
  - src/inmobiliaria/cobros/dto/create-cobro.dto.ts
  - src/inmobiliaria/cobros/dto/register-payment.dto.ts
  - src/inmobiliaria/cobros/dto/index.ts
  - src/inmobiliaria/dispersiones/dispersiones.module.ts
  - src/inmobiliaria/dispersiones/dispersiones.service.ts
  - src/inmobiliaria/dispersiones/dispersiones.controller.ts
  - src/inmobiliaria/dispersiones/dto/index.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Monthly cobros auto-generated from active RENTED consignaciones (POST generate)"
    - "Cobros list with filters by month, status, consignacion, propietario"
    - "Payment registration updates paidAmount and sets status to PAID/PARTIAL"
    - "Late fee calculation: updates daysLate and lateFee based on agency config"
    - "Summary endpoint returns monthly totals (expected, collected, pending, late)"
    - "Reminder tracking: incrementing remindersSent counter"
    - "Dispersiones auto-generated: aggregates paid cobros per propietario for a month"
    - "Dispersion approval workflow: DISP_PENDING -> PROCESSING -> DISP_COMPLETED"
    - "Transfer processing: marks dispersion as completed with transfer reference"
    - "All endpoints scoped to agency"
  artifacts:
    - path: "src/inmobiliaria/cobros/cobros.module.ts"
      provides: "CobrosModule"
      exports: ["CobrosModule"]
    - path: "src/inmobiliaria/dispersiones/dispersiones.module.ts"
      provides: "DispersionesModule"
      exports: ["DispersionesModule"]
  key_links:
    - from: "src/inmobiliaria/cobros/cobros.service.ts"
      to: "prisma.cobro"
      via: "PrismaService"
      pattern: "prisma\\.cobro\\.(create|findMany|update)"
    - from: "src/inmobiliaria/dispersiones/dispersiones.service.ts"
      to: "prisma.dispersion"
      via: "PrismaService"
      pattern: "prisma\\.dispersion\\.(create|findMany|update)"
---

<objective>
Register and verify the Cobros and Dispersiones modules for monthly collection tracking and owner disbursement processing.

Purpose: Both modules already exist with full service implementations. CobrosService handles monthly cobro generation, payment registration, late fees, and reminders. DispersionesService handles disbursement generation (aggregating cobros per propietario), approval, and transfer processing. This plan registers them in AppModule, adds controllers with AgencyMemberGuard, and fixes DTOs.

Output: Working CobrosModule (8 endpoints) and DispersionesModule (5 endpoints).
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-03-SUMMARY.md
@src/inmobiliaria/cobros/cobros.service.ts
@src/inmobiliaria/dispersiones/dispersiones.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and fix CobrosModule with controller and DTOs</name>
  <files>src/inmobiliaria/cobros/cobros.module.ts, src/inmobiliaria/cobros/cobros.controller.ts, src/inmobiliaria/cobros/dto/create-cobro.dto.ts, src/inmobiliaria/cobros/dto/register-payment.dto.ts, src/inmobiliaria/cobros/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register CobrosModule in AppModule**

    2. **Fix CobrosModule**: Declare CobrosController and CobrosService. Export CobrosService (DispersionesModule needs cobro data).

    3. **Fix/create CobrosController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/cobros')`
       - `@ApiTags('inmobiliaria/cobros')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with @Query: month?, status?, consignacionId?, propietarioId?
         - GET /summary - monthly summary with @Query: month (required)
         - GET /cartera-report - aging report (calls updateLateFees first, then returns summary similar to ReportsService.getCarteraReport but simpler)
         - GET /:id - detail
         - POST /generate - generate monthly cobros with @Body { month: string }
         - POST /:id/payment - register payment with RegisterPaymentDto
         - PUT /:id/send-reminder - send reminder (increment counter)
       - Place static routes (summary, cartera-report, generate) BEFORE /:id to avoid route conflicts

    4. **Fix DTOs**:
       - `CreateCobroDto` (or `GenerateCobrosDto`): month!: string (@IsString, @Matches(/^\d{4}-\d{2}$/)) - the month to generate cobros for
       - `RegisterPaymentDto`: paidAmount!: number (@IsInt, @Min(0)), paidDate?: string (@IsOptional, @IsDateString), paymentMethod?: string (@IsOptional, @IsString), paymentReference?: string (@IsOptional, @IsString)
       - `index.ts`: re-export all DTOs

    5. **Verify CobrosService**: The existing service has all needed methods:
       - `generate(agencyId, month)` - creates cobros from RENTED consignaciones
       - `findAll(agencyId, query)` - list with filters
       - `findOne(agencyId, id)` - detail
       - `registerPayment(agencyId, id, dto)` - register payment
       - `getSummary(agencyId, month)` - monthly totals
       - `updateLateFees(agencyId)` - calculate late fees
       - `sendReminder(agencyId, id)` - increment reminder counter

       The service correctly uses enum values with prefixed names (COBRO_PENDING, PAID, PARTIAL, LATE).
       Verify all string literal comparisons like `status: 'ACTIVE'` should use the enum constant instead.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 7-8 cobros endpoints
  </verify>
  <done>
    CobrosModule registered and working: monthly generation, payment tracking, late fees, reminders, summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register and fix DispersionesModule with controller and DTOs</name>
  <files>src/inmobiliaria/dispersiones/dispersiones.module.ts, src/inmobiliaria/dispersiones/dispersiones.controller.ts, src/inmobiliaria/dispersiones/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register DispersionesModule in AppModule**

    2. **Fix DispersionesModule**: Declare controller and service.

    3. **Fix/create DispersionesController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/dispersiones')`
       - `@ApiTags('inmobiliaria/dispersiones')`, `@ApiBearerAuth()`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with @Query: month?, status?, propietarioId?
         - GET /:id - detail with items and propietario
         - POST /generate - generate for month with @Body { month: string }
         - PUT /:id/approve - approve (requires admin or contador role). Gets userId from @CurrentUser('id') and passes as approvedBy
         - PUT /:id/process - process transfer with @Body { transferReference: string }
       - Place /generate route BEFORE /:id

    4. **Create DTOs** if they don't exist:
       - `GenerateDispersionDto`: month!: string (@IsString, @Matches regex YYYY-MM)
       - `ProcessDispersionDto`: transferReference!: string (@IsString, @IsNotEmpty)
       - `index.ts`: re-export

    5. **Verify DispersionesService**: The existing service correctly:
       - Groups cobros by propietario
       - Calculates commission per cobro using consignacion.commissionPercent
       - Creates Dispersion with nested DispersionItems
       - Implements approval and processing workflow with status guards
       - Handles failure by setting FAILED status with failureReason
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Swagger shows 5 dispersiones endpoints
  </verify>
  <done>
    DispersionesModule registered and working: generation from cobros, approval workflow, transfer processing.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Swagger shows 12-13 endpoints total: 7-8 cobros + 5 dispersiones
- Cobro generation creates records from active consignaciones
- Payment registration updates cobro status
- Dispersion generation aggregates cobros per propietario
- Approval workflow enforces status transitions
</verification>

<success_criteria>
CobrosModule and DispersionesModule fully operational. Monthly collection lifecycle works end-to-end: generate cobros -> register payments -> calculate late fees -> generate dispersiones -> approve -> process transfer.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-05-SUMMARY.md`
</output>
