---
phase: 2.2-inmobiliaria-backend
plan: 02
type: execute
wave: 2
depends_on: ["2.2-01"]
files_modified:
  - src/inmobiliaria/agency/agency.module.ts
  - src/inmobiliaria/agency/agency.service.ts
  - src/inmobiliaria/agency/agency.controller.ts
  - src/inmobiliaria/agency/guards/agency-member.guard.ts
  - src/inmobiliaria/agency/decorators/current-agency.decorator.ts
  - src/inmobiliaria/agency/dto/create-agency.dto.ts
  - src/inmobiliaria/agency/dto/update-agency.dto.ts
  - src/inmobiliaria/agency/dto/invite-member.dto.ts
  - src/inmobiliaria/agency/dto/update-member-role.dto.ts
  - src/inmobiliaria/agency/dto/index.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "AgencyModule is registered in AppModule and application starts without errors"
    - "POST /inmobiliaria/agency creates agency and adds creator as ADMIN member"
    - "GET /inmobiliaria/agency returns the user's agency with member count"
    - "PUT /inmobiliaria/agency updates config (admin only)"
    - "Team management: invite, list, update role, remove members"
    - "AgencyMemberGuard sets request.agencyId and request.agencyMemberRole"
    - "@CurrentAgency() decorator extracts agencyId from request"
    - "Integration management: list and toggle integrations"
  artifacts:
    - path: "src/inmobiliaria/agency/agency.module.ts"
      provides: "AgencyModule with controller, service, guard exported"
      exports: ["AgencyModule"]
    - path: "src/inmobiliaria/agency/guards/agency-member.guard.ts"
      provides: "Guard checking agency membership"
      exports: ["AgencyMemberGuard"]
    - path: "src/inmobiliaria/agency/decorators/current-agency.decorator.ts"
      provides: "Decorator extracting agencyId from request"
      exports: ["CurrentAgency"]
  key_links:
    - from: "src/app.module.ts"
      to: "src/inmobiliaria/agency/agency.module.ts"
      via: "imports array"
      pattern: "AgencyModule"
    - from: "src/inmobiliaria/agency/agency.controller.ts"
      to: "src/inmobiliaria/agency/agency.service.ts"
      via: "constructor injection"
      pattern: "AgencyService"
---

<objective>
Register the Agency core module in AppModule and verify all agency endpoints work correctly. Fix any issues in existing code.

Purpose: The AgencyModule already exists with full implementation (service, controller, DTOs, guard, decorator) but is NOT registered in AppModule. This plan registers it, verifies the code compiles, and fixes any issues found.

Output: Working AgencyModule registered in AppModule with all 8+ endpoints functional.
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-01-SUMMARY.md
@src/inmobiliaria/agency/agency.module.ts
@src/inmobiliaria/agency/agency.service.ts
@src/inmobiliaria/agency/agency.controller.ts
@src/inmobiliaria/agency/guards/agency-member.guard.ts
@src/inmobiliaria/agency/decorators/current-agency.decorator.ts
@src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register AgencyModule in AppModule and verify compilation</name>
  <files>src/app.module.ts, src/inmobiliaria/agency/agency.module.ts</files>
  <action>
    1. Add import for `AgencyModule` from `./inmobiliaria/agency/agency.module.js` in `src/app.module.ts`
    2. Add `AgencyModule` to the `imports` array in `@Module` decorator
    3. Verify that `AgencyModule` correctly imports `PrismaModule` (or relies on global PrismaModule). Check if PrismaModule is global - it is (Phase 1 setup). The AgencyModule currently doesn't import PrismaModule explicitly; verify PrismaService is injectable via global module.
    4. Verify the `AgencyMemberGuard` works: it imports PrismaService and AgencyMemberStatus - check these imports use `.js` extension for ESM compatibility
    5. Verify the `CurrentAgency` decorator: check it creates a `createParamDecorator` that reads `request.agencyId` (set by AgencyMemberGuard)
    6. Ensure `AgencyModule` exports `AgencyService` so other inmobiliaria modules can inject it (it already does based on existing code)
    7. Run `npx tsc --noEmit` to verify no TypeScript compilation errors
    8. Run `npm run start:dev` briefly to verify the server starts and agency endpoints are registered in Swagger

    The guard and decorator need to be verified:
    - `AgencyMemberGuard` should look up the user's agency membership in AgencyMember table
    - It should set `request.agencyId` and `request.agencyMemberRole`
    - `CurrentAgency` decorator should use `createParamDecorator` to extract these values

    Fix any issues found during compilation.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with zero errors related to agency module
    - Server starts with `npm run start:dev`
    - Swagger shows routes under `/inmobiliaria/agency/*`
  </verify>
  <done>
    AgencyModule registered in AppModule. Server starts. All agency endpoints visible in Swagger. Guard and decorator work correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix agency DTOs and service methods</name>
  <files>src/inmobiliaria/agency/dto/create-agency.dto.ts, src/inmobiliaria/agency/dto/update-agency.dto.ts, src/inmobiliaria/agency/dto/invite-member.dto.ts, src/inmobiliaria/agency/dto/update-member-role.dto.ts, src/inmobiliaria/agency/dto/index.ts</files>
  <action>
    Review and fix all DTOs for correctness:

    1. `CreateAgencyDto`: Must have class-validator decorators with `!:` definite assignment. Fields: name (required string), nit (optional string), address (optional), city (optional), phone (optional), email (optional), defaultCommissionPercent (optional number, default 10). Should use `@IsString()`, `@IsOptional()`, `@IsNumber()`, `@ApiProperty()`.

    2. `UpdateAgencyDto`: Same fields as CreateAgencyDto but all optional. Add agency config fields: defaultLateFeePercent, paymentDueDay, disbursementDay, reminderDaysBefore, reminderDaysAfter, branding, legalRepresentative, legalDocumentNumber, logoUrl.

    3. `InviteMemberDto`: email (required, @IsEmail()), role (required, @IsEnum(AgencyMemberRole)). For agent-specific fields: agentRole, specialization, commissionSplit, zone, hireDate (all optional, only relevant when role=AGENTE).

    4. `UpdateMemberRoleDto`: role (required, @IsEnum(AgencyMemberRole)).

    5. `index.ts`: Must re-export all DTOs.

    For each DTO:
    - Use `!:` (definite assignment) not `?:` for required fields (project convention)
    - Use `@ApiProperty()` / `@ApiPropertyOptional()` for Swagger
    - Use class-validator decorators (@IsString, @IsOptional, @IsEnum, @IsNumber, @IsEmail)
    - Import enums from `../../common/enums/*.enum.js`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - DTOs have correct class-validator decorators
    - All DTOs re-exported from index.ts
  </verify>
  <done>
    All 4 agency DTOs have correct class-validator decorators, Swagger annotations, and definite assignment syntax. Index barrel exports all DTOs.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Server starts with `npm run start:dev`
- Swagger at `/api` shows all agency endpoints under `inmobiliaria/agency` tag
- Agency creation, listing, updating, member management routes are present
</verification>

<success_criteria>
AgencyModule is fully operational: registered in AppModule, all DTOs validated, all endpoints accessible. Guard and decorator functional for downstream modules.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-02-SUMMARY.md`
</output>
