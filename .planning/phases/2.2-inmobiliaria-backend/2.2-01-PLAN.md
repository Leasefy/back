---
phase: 2.2-inmobiliaria-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - supabase/migrations/00004_inmobiliaria_schema.sql
  - scripts/run-migration-2.2.mjs
autonomous: true

must_haves:
  truths:
    - "All 18 inmobiliaria models exist in Prisma schema with correct fields, relations, and indexes"
    - "All ~20 inmobiliaria enums exist in Prisma schema matching frontend data models"
    - "Database migration runs successfully creating all tables and enums"
    - "Prisma client generates without errors after migration"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "18 inmobiliaria models + 20 enums"
      contains: "model Agency"
    - path: "supabase/migrations/00004_inmobiliaria_schema.sql"
      provides: "SQL migration for inmobiliaria tables"
      contains: "CREATE TABLE"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Supabase PostgreSQL"
      via: "prisma db push or migration"
      pattern: "model (Agency|Propietario|Consignacion|PipelineItem|Cobro|Dispersion)"
---

<objective>
Validate and finalize the database schema for all 18 inmobiliaria models and generate/run the Prisma migration.

Purpose: The schema already exists in `prisma/schema.prisma` with all 18 models and ~20 enums defined. This plan validates the schema is correct, generates the migration SQL, and pushes it to the database so all subsequent plans can use the models.

Output: Validated Prisma schema, generated migration, database tables created.
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Prisma schema and generate migration</name>
  <files>prisma/schema.prisma, supabase/migrations/00004_inmobiliaria_schema.sql, scripts/run-migration-2.2.mjs</files>
  <action>
    The schema already has all 18 inmobiliaria models and ~20 enums defined at the bottom of `prisma/schema.prisma` (starting around line 1192). They were added during research phase.

    Steps:
    1. Run `npx prisma validate` to ensure schema is valid
    2. Run `npx prisma generate` to regenerate the Prisma client with the new models
    3. Run `npx prisma db push --accept-data-loss` to push schema to the database (or create a migration)
    4. If db push fails due to enum naming conflicts (CobroStatus values prefixed to avoid PostgreSQL conflicts), check and fix any issues
    5. Create `supabase/migrations/00004_inmobiliaria_schema.sql` with the SQL diff (run `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` to generate the SQL)
    6. Create `scripts/run-migration-2.2.mjs` following the pattern from `scripts/run-migration.mjs` - a helper script that runs the inmobiliaria migration

    Key schema notes:
    - Some enum values are prefixed to avoid PostgreSQL conflicts (e.g., COBRO_PENDING instead of PENDING, DISP_PENDING, MAINT_APPROVED, etc.)
    - Agency model uses JSON fields for branding, reminderDaysBefore, reminderDaysAfter
    - ActaEntrega uses JSON fields for rooms, items, meterReadings, keysDelivered, signatures, deductions
    - All UUID fields use `@db.Uuid` and `@default(uuid())`
    - All models use `@@map()` with snake_case table names

    If `npx prisma validate` or `npx prisma generate` reveals any issues, fix them in the schema before proceeding.
  </action>
  <verify>
    - `npx prisma validate` exits with code 0
    - `npx prisma generate` exits with code 0
    - `npx prisma db push` succeeds (or migration runs successfully)
    - The generated migration SQL file exists
  </verify>
  <done>
    Prisma schema validates, client generates, database has all 18 inmobiliaria tables and ~20 enums. Migration SQL is saved for reproducibility.
  </done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes
- `npx prisma generate` passes
- Database contains all 18 new tables: agencies, agency_members, propietarios, consignaciones, pipeline_items, cobros, dispersiones, dispersion_items, solicitudes_mantenimiento, mantenimiento_quotes, renovaciones, renovacion_history, actas_entrega, agency_document_templates, agency_documents, agency_integrations
- All enum types created in PostgreSQL
</verification>

<success_criteria>
All 18 inmobiliaria database tables exist and are accessible via Prisma client. Schema validates and generates without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-01-SUMMARY.md`
</output>
