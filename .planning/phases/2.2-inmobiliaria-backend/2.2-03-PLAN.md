---
phase: 2.2-inmobiliaria-backend
plan: 03
type: execute
wave: 3
depends_on: ["2.2-02"]
files_modified:
  - src/inmobiliaria/propietarios/propietarios.module.ts
  - src/inmobiliaria/propietarios/propietarios.service.ts
  - src/inmobiliaria/propietarios/propietarios.controller.ts
  - src/inmobiliaria/propietarios/dto/create-propietario.dto.ts
  - src/inmobiliaria/propietarios/dto/update-propietario.dto.ts
  - src/inmobiliaria/propietarios/dto/index.ts
  - src/inmobiliaria/consignaciones/consignaciones.module.ts
  - src/inmobiliaria/consignaciones/consignaciones.service.ts
  - src/inmobiliaria/consignaciones/consignaciones.controller.ts
  - src/inmobiliaria/consignaciones/dto/create-consignacion.dto.ts
  - src/inmobiliaria/consignaciones/dto/update-consignacion.dto.ts
  - src/inmobiliaria/consignaciones/dto/index.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Propietarios CRUD works: create, list (with search), get detail, update, delete"
    - "Propietarios list returns computed fields: propertyCount, activeLeases, totalMonthlyRent"
    - "Propietario extracto endpoint returns monthly statement with line items and totals"
    - "Consignaciones CRUD works: create, list (with filters), get detail, update, delete"
    - "Consignacion assign-agent endpoint works"
    - "Consignacion lease-history endpoint returns cobros"
    - "All endpoints scoped to agency via AgencyMemberGuard"
    - "Both modules registered in AppModule"
  artifacts:
    - path: "src/inmobiliaria/propietarios/propietarios.module.ts"
      provides: "PropietariosModule"
      exports: ["PropietariosModule"]
    - path: "src/inmobiliaria/consignaciones/consignaciones.module.ts"
      provides: "ConsignacionesModule"
      exports: ["ConsignacionesModule"]
  key_links:
    - from: "src/inmobiliaria/propietarios/propietarios.controller.ts"
      to: "src/inmobiliaria/agency/guards/agency-member.guard.ts"
      via: "@UseGuards(AgencyMemberGuard)"
      pattern: "AgencyMemberGuard"
    - from: "src/inmobiliaria/consignaciones/consignaciones.service.ts"
      to: "prisma.consignacion"
      via: "PrismaService"
      pattern: "prisma\\.(propietario|consignacion)"
---

<objective>
Register and verify the Propietarios and Consignaciones modules with full CRUD, extracto generation, agent assignment, and lease history.

Purpose: Both modules already exist with service, controller, and DTO implementations. This plan registers them in AppModule, verifies controllers use the AgencyMemberGuard for scoping, fixes DTOs with proper class-validator decorators, and ensures all endpoints compile and work.

Output: Two fully functional modules: PropietariosModule (6 endpoints) and ConsignacionesModule (8 endpoints).
</objective>

<execution_context>
@/home/victo/.claude/get-shit-done/workflows/execute-plan.md
@/home/victo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-RESEARCH.md
@.planning/phases/2.2-inmobiliaria-backend/2.2-02-SUMMARY.md
@src/inmobiliaria/propietarios/propietarios.service.ts
@src/inmobiliaria/propietarios/propietarios.controller.ts
@src/inmobiliaria/consignaciones/consignaciones.service.ts
@src/inmobiliaria/consignaciones/consignaciones.controller.ts
@src/inmobiliaria/agency/guards/agency-member.guard.ts
@src/inmobiliaria/agency/decorators/current-agency.decorator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix and register PropietariosModule with DTOs and controller</name>
  <files>src/inmobiliaria/propietarios/propietarios.module.ts, src/inmobiliaria/propietarios/propietarios.controller.ts, src/inmobiliaria/propietarios/dto/create-propietario.dto.ts, src/inmobiliaria/propietarios/dto/update-propietario.dto.ts, src/inmobiliaria/propietarios/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register in AppModule**: Add `PropietariosModule` import and add to `imports` array in `src/app.module.ts`

    2. **Fix PropietariosModule**: Ensure it imports `AgencyModule` (for AgencyMemberGuard dependency) and `PrismaModule` if needed (PrismaModule is global so likely not needed). It should declare `PropietariosController` and `PropietariosService`.

    3. **Fix PropietariosController**:
       - Must use `@UseGuards(AgencyMemberGuard)` at class level
       - Must use `@CurrentAgency('agencyId')` decorator (from `../../agency/decorators/current-agency.decorator.js`) to get the agencyId in each handler instead of looking up agency per request
       - Routes should be under `@Controller('inmobiliaria/propietarios')`
       - Endpoints: GET /, GET /:id, POST /, PUT /:id, DELETE /:id, GET /:id/extracto
       - The extracto endpoint needs a `@Query('month')` parameter (format: YYYY-MM)
       - All IDs validated with `@Param('id', ParseUUIDPipe)`
       - Add `@ApiTags('inmobiliaria/propietarios')`, `@ApiBearerAuth()`

    4. **Fix DTOs**:
       - `CreatePropietarioDto`: name!: string (@IsString, @IsNotEmpty), email?: string (@IsOptional, @IsEmail), phone?: string (@IsOptional, @IsString), documentType!: PropietarioDocumentType (@IsEnum), documentNumber!: string (@IsString), address?: string, city?: string, bankName?: string, bankAccountType?: string, bankAccountNumber?: string, bankAccountHolder?: string, notes?: string, tags?: string[] (@IsOptional, @IsArray)
       - `UpdatePropietarioDto`: All fields optional (use PartialType from @nestjs/swagger if available, or manually make all optional)
       - Use `!:` for required fields, `?:` for optional with `@IsOptional()`
       - Import `PropietarioDocumentType` from `../../../common/enums/propietario-document-type.enum.js`

    5. **Verify service**: The existing `PropietariosService` looks correct. It computes propertyCount, activeLeases, totalMonthlyRent from consignaciones. The extracto method fetches cobros and calculates commissions. Verify all method signatures match what the controller calls.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Server starts, Swagger shows 6 propietarios endpoints
  </verify>
  <done>
    PropietariosModule registered and working: CRUD + extracto. All 6 endpoints use AgencyMemberGuard. DTOs have proper validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix and register ConsignacionesModule with DTOs and controller</name>
  <files>src/inmobiliaria/consignaciones/consignaciones.module.ts, src/inmobiliaria/consignaciones/consignaciones.controller.ts, src/inmobiliaria/consignaciones/dto/create-consignacion.dto.ts, src/inmobiliaria/consignaciones/dto/update-consignacion.dto.ts, src/inmobiliaria/consignaciones/dto/index.ts, src/app.module.ts</files>
  <action>
    1. **Register in AppModule**: Add `ConsignacionesModule` import and add to `imports` array

    2. **Fix ConsignacionesModule**: Declare controller and service. Export service (other modules like CobrosModule need it).

    3. **Fix ConsignacionesController**:
       - `@UseGuards(AgencyMemberGuard)` at class level
       - `@Controller('inmobiliaria/consignaciones')`
       - Use `@CurrentAgency('agencyId')` decorator
       - Endpoints:
         - GET / - list with query filters: status, availability, agenteUserId, search
         - GET /:id - detail
         - POST / - create
         - PUT /:id - update
         - DELETE /:id - delete
         - PUT /:id/assign-agent - with body { agenteUserId: string }
         - GET /:id/lease-history - cobros history
         - GET /:id/maintenance - maintenance list (NOTE: this endpoint is in the research but NOT in the existing service - add it or delegate to MantenimientoService later)
       - For the maintenance endpoint, add a simple method that queries solicitudMantenimiento by consignacionId

    4. **Fix DTOs**:
       - `CreateConsignacionDto`: propietarioId!: string (@IsUUID), propertyId?: string (@IsOptional, @IsUUID), agenteUserId?: string (@IsOptional, @IsUUID), propertyTitle!: string, propertyAddress!: string, propertyCity!: string, propertyZone?: string, propertyType?: ConsignacionPropertyType (@IsOptional, @IsEnum), propertyThumbnail?: string, monthlyRent!: number (@IsInt, @Min(0)), adminFee?: number (@IsOptional, @IsInt), commissionPercent!: number (@IsNumber), contractDate!: string (@IsDateString), contractEndDate?: string (@IsOptional, @IsDateString), minimumTerm?: number (@IsOptional, @IsInt)
       - `UpdateConsignacionDto`: All optional. Include additional fields: status, availability, currentLeaseId, currentTenantName, leaseEndDate, consignmentContractUrl

    5. **Add maintenance endpoint**: In `ConsignacionesService`, add a `getMaintenanceRequests(agencyId, consignacionId)` method that queries `solicitudMantenimiento` by consignacionId. In the controller, add `GET /:id/maintenance`.

    6. **Verify service**: The existing service looks correct for CRUD + assignAgent + getLeaseHistory. Verify all imports use `.js` extensions.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Server starts, Swagger shows 8 consignaciones endpoints
  </verify>
  <done>
    ConsignacionesModule registered and working: CRUD + assign-agent + lease-history + maintenance. All 8 endpoints scoped to agency.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Swagger shows 14 endpoints total: 6 propietarios + 8 consignaciones
- All endpoints require authentication and agency membership
</verification>

<success_criteria>
PropietariosModule and ConsignacionesModule fully operational: registered in AppModule, all endpoints accessible, DTOs validated, services correct. Propietario extracto returns monthly statement. Consignacion supports agent assignment and lease history.
</success_criteria>

<output>
After completion, create `.planning/phases/2.2-inmobiliaria-backend/2.2-03-SUMMARY.md`
</output>
