---
phase: 03-properties
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/properties/properties.module.ts
  - src/properties/properties.service.ts
  - src/properties/properties.controller.ts
  - src/properties/dto/create-property.dto.ts
  - src/properties/dto/update-property.dto.ts
  - src/app.module.ts
autonomous: true

must_haves:
  truths:
    - "Landlord can create a property with all required fields"
    - "Landlord can update their own property"
    - "Landlord can delete their own property"
    - "Landlord can view list of their own properties"
    - "Non-owner cannot update or delete property"
  artifacts:
    - path: "src/properties/properties.module.ts"
      provides: "Properties NestJS module"
      exports: ["PropertiesModule"]
    - path: "src/properties/properties.service.ts"
      provides: "Property CRUD operations with ownership checks"
      exports: ["PropertiesService"]
    - path: "src/properties/properties.controller.ts"
      provides: "Landlord property management endpoints"
      exports: ["PropertiesController"]
    - path: "src/properties/dto/create-property.dto.ts"
      provides: "DTO with validation for property creation"
      exports: ["CreatePropertyDto"]
    - path: "src/properties/dto/update-property.dto.ts"
      provides: "DTO for property updates (partial)"
      exports: ["UpdatePropertyDto"]
  key_links:
    - from: "src/properties/properties.controller.ts"
      to: "src/properties/properties.service.ts"
      via: "constructor injection"
      pattern: "constructor.*PropertiesService"
    - from: "src/properties/properties.service.ts"
      to: "src/database/prisma.service.ts"
      via: "constructor injection"
      pattern: "this.prisma.property"
    - from: "src/app.module.ts"
      to: "src/properties/properties.module.ts"
      via: "imports array"
      pattern: "PropertiesModule"
---

<objective>
Create PropertiesModule with landlord CRUD operations and ownership validation.

Purpose: Enable landlords to manage their property listings.
Output: Working endpoints for create, update, delete, and list own properties.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-properties/03-CONTEXT.md
@.planning/phases/03-properties/03-01-SUMMARY.md
@prisma/schema.prisma
@src/users/users.controller.ts
@src/users/users.service.ts
@src/users/dto/update-profile.dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DTOs with validation</name>
  <files>
    src/properties/dto/create-property.dto.ts
    src/properties/dto/update-property.dto.ts
    src/properties/dto/index.ts
  </files>
  <action>
Create DTOs following the pattern from users/dto:

**create-property.dto.ts:**
```typescript
import { ApiProperty, ApiPropertyOptional } from '@nestjs/swagger';
import {
  IsString,
  IsEnum,
  IsNumber,
  IsOptional,
  IsArray,
  Min,
  Max,
  MaxLength,
  MinLength,
  IsLatitude,
  IsLongitude,
} from 'class-validator';
import { PropertyType, PropertyStatus, ListingPlan } from '../../common/enums/index.js';

// Valid amenity IDs
const VALID_AMENITIES = [
  'pool', 'gym', 'security', 'parking', 'elevator', 'terrace',
  'bbq', 'playground', 'laundry', 'pets', 'furnished', 'balcony',
  'storage', 'ac', 'heating'
] as const;

export class CreatePropertyDto {
  @ApiProperty({ example: 'Apartamento moderno en Chapinero' })
  @IsString()
  @MinLength(5)
  @MaxLength(100)
  title: string;

  @ApiProperty({ example: 'Hermoso apartamento con vista a la ciudad...' })
  @IsString()
  @MinLength(20)
  @MaxLength(5000)
  description: string;

  @ApiProperty({ enum: PropertyType, example: PropertyType.APARTMENT })
  @IsEnum(PropertyType)
  type: PropertyType;

  @ApiPropertyOptional({ enum: PropertyStatus, default: PropertyStatus.DRAFT })
  @IsOptional()
  @IsEnum(PropertyStatus)
  status?: PropertyStatus;

  @ApiProperty({ example: 'Bogota' })
  @IsString()
  @MaxLength(50)
  city: string;

  @ApiProperty({ example: 'Chapinero' })
  @IsString()
  @MaxLength(100)
  neighborhood: string;

  @ApiProperty({ example: 'Calle 53 #7-25' })
  @IsString()
  @MaxLength(200)
  address: string;

  @ApiPropertyOptional({ example: 4.6534 })
  @IsOptional()
  @IsLatitude()
  latitude?: number;

  @ApiPropertyOptional({ example: -74.0594 })
  @IsOptional()
  @IsLongitude()
  longitude?: number;

  @ApiProperty({ example: 2500000, description: 'Monthly rent in COP' })
  @IsNumber()
  @Min(100000)
  @Max(100000000)
  monthlyRent: number;

  @ApiPropertyOptional({ example: 150000, default: 0 })
  @IsOptional()
  @IsNumber()
  @Min(0)
  adminFee?: number;

  @ApiPropertyOptional({ example: 2500000, default: 0 })
  @IsOptional()
  @IsNumber()
  @Min(0)
  deposit?: number;

  @ApiProperty({ example: 2 })
  @IsNumber()
  @Min(0)
  @Max(20)
  bedrooms: number;

  @ApiProperty({ example: 2 })
  @IsNumber()
  @Min(1)
  @Max(10)
  bathrooms: number;

  @ApiProperty({ example: 75, description: 'Area in square meters' })
  @IsNumber()
  @Min(10)
  @Max(10000)
  area: number;

  @ApiPropertyOptional({ example: 5 })
  @IsOptional()
  @IsNumber()
  @Min(0)
  @Max(100)
  floor?: number;

  @ApiPropertyOptional({ example: 1 })
  @IsOptional()
  @IsNumber()
  @Min(0)
  @Max(10)
  parkingSpaces?: number;

  @ApiPropertyOptional({ example: 4, description: 'Colombian stratum 1-6' })
  @IsOptional()
  @IsNumber()
  @Min(1)
  @Max(6)
  stratum?: number;

  @ApiPropertyOptional({ example: 2015 })
  @IsOptional()
  @IsNumber()
  @Min(1900)
  @Max(2100)
  yearBuilt?: number;

  @ApiPropertyOptional({ example: ['pool', 'gym', 'parking'], type: [String] })
  @IsOptional()
  @IsArray()
  @IsString({ each: true })
  amenities?: string[];

  @ApiPropertyOptional({ enum: ListingPlan, default: ListingPlan.FREE })
  @IsOptional()
  @IsEnum(ListingPlan)
  listingPlan?: ListingPlan;
}
```

**update-property.dto.ts:**
```typescript
import { PartialType } from '@nestjs/swagger';
import { CreatePropertyDto } from './create-property.dto.js';

export class UpdatePropertyDto extends PartialType(CreatePropertyDto) {}
```

**index.ts:**
```typescript
export * from './create-property.dto.js';
export * from './update-property.dto.js';
```
  </action>
  <verify>`npx tsc --noEmit` passes</verify>
  <done>DTOs created with full validation for all property fields</done>
</task>

<task type="auto">
  <name>Task 2: Create PropertiesService with ownership validation</name>
  <files>src/properties/properties.service.ts</files>
  <action>
Create PropertiesService following the pattern from UsersService:

```typescript
import { Injectable, NotFoundException, ForbiddenException } from '@nestjs/common';
import { PrismaService } from '../database/prisma.service.js';
import { CreatePropertyDto, UpdatePropertyDto } from './dto/index.js';
import type { Property } from '@prisma/client';

@Injectable()
export class PropertiesService {
  constructor(private readonly prisma: PrismaService) {}

  /**
   * Create a new property for a landlord
   */
  async create(landlordId: string, dto: CreatePropertyDto): Promise<Property> {
    // Validate amenities if provided
    if (dto.amenities) {
      this.validateAmenities(dto.amenities);
    }

    return this.prisma.property.create({
      data: {
        ...dto,
        landlordId,
      },
      include: {
        images: {
          orderBy: { order: 'asc' },
        },
      },
    });
  }

  /**
   * Update a property (only by owner)
   */
  async update(
    propertyId: string,
    landlordId: string,
    dto: UpdatePropertyDto,
  ): Promise<Property> {
    const property = await this.findByIdOrThrow(propertyId);
    this.ensureOwnership(property, landlordId);

    // TODO: Check for active applications when Phase 4 implemented
    // await this.checkNoActiveApplications(propertyId);

    // Validate amenities if provided
    if (dto.amenities) {
      this.validateAmenities(dto.amenities);
    }

    return this.prisma.property.update({
      where: { id: propertyId },
      data: dto,
      include: {
        images: {
          orderBy: { order: 'asc' },
        },
      },
    });
  }

  /**
   * Delete a property (only by owner, blocked if has active applications)
   */
  async delete(propertyId: string, landlordId: string): Promise<void> {
    const property = await this.findByIdOrThrow(propertyId);
    this.ensureOwnership(property, landlordId);

    // TODO: Check for active applications when Phase 4 implemented
    // await this.checkNoActiveApplications(propertyId);

    await this.prisma.property.delete({
      where: { id: propertyId },
    });
  }

  /**
   * Get all properties for a landlord
   */
  async findByLandlord(landlordId: string): Promise<Property[]> {
    return this.prisma.property.findMany({
      where: { landlordId },
      include: {
        images: {
          orderBy: { order: 'asc' },
        },
      },
      orderBy: { createdAt: 'desc' },
    });
  }

  /**
   * Get property by ID (for internal use)
   */
  async findById(propertyId: string): Promise<Property | null> {
    return this.prisma.property.findUnique({
      where: { id: propertyId },
      include: {
        images: {
          orderBy: { order: 'asc' },
        },
      },
    });
  }

  /**
   * Get property by ID or throw NotFoundException
   */
  async findByIdOrThrow(propertyId: string): Promise<Property> {
    const property = await this.findById(propertyId);
    if (!property) {
      throw new NotFoundException(`Property with ID ${propertyId} not found`);
    }
    return property;
  }

  /**
   * Validate that user owns the property
   */
  private ensureOwnership(property: Property, landlordId: string): void {
    if (property.landlordId !== landlordId) {
      throw new ForbiddenException('You do not own this property');
    }
  }

  /**
   * Validate amenity IDs against allowed list
   */
  private validateAmenities(amenities: string[]): void {
    const validAmenities = new Set([
      'pool', 'gym', 'security', 'parking', 'elevator', 'terrace',
      'bbq', 'playground', 'laundry', 'pets', 'furnished', 'balcony',
      'storage', 'ac', 'heating'
    ]);

    for (const amenity of amenities) {
      if (!validAmenities.has(amenity)) {
        throw new ForbiddenException(`Invalid amenity: ${amenity}`);
      }
    }
  }
}
```
  </action>
  <verify>Service file compiles without errors</verify>
  <done>PropertiesService created with CRUD methods and ownership validation</done>
</task>

<task type="auto">
  <name>Task 3: Create PropertiesController, module, and register in app</name>
  <files>
    src/properties/properties.controller.ts
    src/properties/properties.module.ts
    src/app.module.ts
  </files>
  <action>
**properties.controller.ts:**
```typescript
import {
  Controller,
  Get,
  Post,
  Patch,
  Delete,
  Body,
  Param,
  ParseUUIDPipe,
  HttpCode,
  HttpStatus,
} from '@nestjs/common';
import {
  ApiTags,
  ApiBearerAuth,
  ApiOperation,
  ApiCreatedResponse,
  ApiOkResponse,
  ApiNoContentResponse,
} from '@nestjs/swagger';
import type { User, Property } from '@prisma/client';
import { CurrentUser } from '../auth/decorators/current-user.decorator.js';
import { Roles } from '../auth/decorators/roles.decorator.js';
import { Role } from '../common/enums/role.enum.js';
import { PropertiesService } from './properties.service.js';
import { CreatePropertyDto, UpdatePropertyDto } from './dto/index.js';

/**
 * Controller for landlord property management.
 * All endpoints require LANDLORD or BOTH role.
 */
@ApiTags('properties')
@ApiBearerAuth()
@Controller('properties')
export class PropertiesController {
  constructor(private readonly propertiesService: PropertiesService) {}

  /**
   * Create a new property listing.
   */
  @Post()
  @Roles(Role.LANDLORD, Role.BOTH)
  @ApiOperation({ summary: 'Create a new property' })
  @ApiCreatedResponse({ description: 'Property created successfully' })
  async create(
    @CurrentUser('id') landlordId: string,
    @Body() dto: CreatePropertyDto,
  ): Promise<Property> {
    return this.propertiesService.create(landlordId, dto);
  }

  /**
   * Get all properties owned by the authenticated landlord.
   */
  @Get('mine')
  @Roles(Role.LANDLORD, Role.BOTH)
  @ApiOperation({ summary: 'Get my properties' })
  @ApiOkResponse({ description: 'List of landlord properties' })
  async findMine(@CurrentUser('id') landlordId: string): Promise<Property[]> {
    return this.propertiesService.findByLandlord(landlordId);
  }

  /**
   * Update a property.
   */
  @Patch(':id')
  @Roles(Role.LANDLORD, Role.BOTH)
  @ApiOperation({ summary: 'Update a property' })
  @ApiOkResponse({ description: 'Property updated successfully' })
  async update(
    @Param('id', ParseUUIDPipe) propertyId: string,
    @CurrentUser('id') landlordId: string,
    @Body() dto: UpdatePropertyDto,
  ): Promise<Property> {
    return this.propertiesService.update(propertyId, landlordId, dto);
  }

  /**
   * Delete a property.
   */
  @Delete(':id')
  @Roles(Role.LANDLORD, Role.BOTH)
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({ summary: 'Delete a property' })
  @ApiNoContentResponse({ description: 'Property deleted successfully' })
  async delete(
    @Param('id', ParseUUIDPipe) propertyId: string,
    @CurrentUser('id') landlordId: string,
  ): Promise<void> {
    return this.propertiesService.delete(propertyId, landlordId);
  }
}
```

**properties.module.ts:**
```typescript
import { Module } from '@nestjs/common';
import { PropertiesController } from './properties.controller.js';
import { PropertiesService } from './properties.service.js';

@Module({
  controllers: [PropertiesController],
  providers: [PropertiesService],
  exports: [PropertiesService],
})
export class PropertiesModule {}
```

**Update app.module.ts:**
Add import statement at top:
```typescript
import { PropertiesModule } from './properties/properties.module.js';
```

Add to imports array:
```typescript
imports: [
  // ... existing imports
  PropertiesModule,
],
```
  </action>
  <verify>
    1. `npm run build` succeeds
    2. `npm run start:dev` starts without errors
    3. Swagger at /api shows properties endpoints
  </verify>
  <done>PropertiesController and module created, registered in app.module</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run start:dev` starts without errors
3. Swagger shows POST /properties, GET /properties/mine, PATCH /properties/:id, DELETE /properties/:id
4. All endpoints require authentication (return 401 without token)
5. All endpoints require LANDLORD or BOTH role (return 403 for TENANT)
</verification>

<success_criteria>
- POST /properties creates property with landlordId from JWT
- GET /properties/mine returns only properties owned by authenticated user
- PATCH /properties/:id updates property only if user owns it
- DELETE /properties/:id deletes property only if user owns it
- Non-owner receives 403 Forbidden
- Invalid property ID returns 404 Not Found
- Validation errors return 400 Bad Request with details
</success_criteria>

<output>
After completion, create `.planning/phases/03-properties/03-02-SUMMARY.md`
</output>
